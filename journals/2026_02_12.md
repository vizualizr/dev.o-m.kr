date-created:: [[2026-02-12]]
date-updated::
division::
stack::
tags:: gsap.to()
type::
public:: true
ai-sourced:: #ai-proofed

- ## Summary
	- GSAP에서 SVG 요소의 좌표 이동 방식 학습했다. [[SVG 요소의 크기와 위치를 제어하는 두 가지 방법. 요소 속성과 CSS transform]]을 참조하라
- ## Steps
	-
- ## Troubleshooting
	- ### `gsap.to()`의 이해
		-
	- GSAP에서 SVG 요소의 좌표 이동 방식
		- CSS Transform
			- GSAP은 기본적으로 "애니메이션 = 위치 이동"이라고 가정하고, 가장 **빠르고 부드러운** 방식인 `transform`을 우선 사용한다.
			- `transform`은 브라우저의 **GPU(Compositor Layer)**가 처리하기 때문에 메인 스레드 부하가 적다.
		- SVG 요소의 속성을 직접 변경
			- `AttrPlugin`을 사용하면 실제 좌표를 바꿔서 요소의 위치를 바꾼다. 아래 GSAP 주의 사항을 참조하라.
				- Animating CSS
				  Do not attempt to animate CSS-related properties inside the attr object. GSAP handles [CSS](https://gsap.com/docs/v3/GSAP/CorePlugins/CSS) differently internally. In the example above, the `x` outside the `attr:{}` object will animate the CSS transform, whereas the x inside the attr object will animate the underlying geometry - the x coordinate of the rect element.
				  https://gsap.com/docs/v3/GSAP/CorePlugins/Attributes/
				-
			-
- ## log
	- [[2026-02-12]] Page created.
- ### References
	- [[SVG 요소의 크기와 위치를 제어하는 두 가지 방법. 요소 속성과 CSS transform]]
	- [gsap.to() | GSAP | Docs &amp; Learning](https://gsap.com/docs/v3/GSAP/gsap.to()/)
	- [Attributes | GSAP | Docs &amp; Learning](https://gsap.com/docs/v3/GSAP/CorePlugins/Attributes/)